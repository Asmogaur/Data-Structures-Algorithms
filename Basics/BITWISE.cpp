#include <iostream>
using namespace std;

int main() {
    
    int a = 2;
    int b = 4;

    cout << "AND of a,b : " << (a&b) << endl;
    cout << "OR of a,b : " << (a|b) << endl;
    cout << "NOT of a : " << (~a) << endl;
    cout << "XOR of a,b : " << (a^b) << endl;
}
